{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% block content -%}
    <h1>BannerCategory creation</h1>

    <form action="{{ path('bannercategory_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('bannercategory') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
